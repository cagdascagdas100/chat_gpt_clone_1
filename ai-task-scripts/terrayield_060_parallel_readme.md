# TerraYield 060 Parallel Run

Three subprojects to run concurrently with max parallel limit 5:

1. backend_db_api_sync
2. frontend_admin_ui
3. ops_performance_release

This file documents the run request. The executable task is queued through current-task.json.
