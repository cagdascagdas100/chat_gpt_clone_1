# Hosted cloud readiness status

Verified now:

- Local runtime smoke passed.
- Targeted tests passed.
- Public hosted URL is not verified yet.

Current classification:

- LOCAL_READY
- CLOUD_READY_PENDING_PROVIDER

Required before claiming browser-only hosted operation:

- Hosted API URL.
- Hosted web UI URL.
- Managed Postgres or equivalent database.
- Cloud data storage or mapped read-only datasets.
- Runtime smoke against the hosted URL.

Free-tier note:

A free tier can be used as a best-effort target, but it is not the same as unlimited free production hosting.
