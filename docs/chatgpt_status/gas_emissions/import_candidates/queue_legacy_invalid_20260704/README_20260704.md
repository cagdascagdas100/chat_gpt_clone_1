# Gas Emissions Legacy Queue Archive - 2026-07-04

These files were moved out of the active queue because they were historical/incomplete queue records without the current shared-runner contract fields, especially allowed_paths. Keeping them in docs/chatgpt_status/gas_emissions/queue could make ChatGPT or the shared runner report confusing pending/default queue items.

The active queue path now keeps only current contract queue files. These archived files are evidence/import candidates, not runnable queue items.