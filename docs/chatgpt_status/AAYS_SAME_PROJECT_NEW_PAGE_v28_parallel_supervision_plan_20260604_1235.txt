PAGE_KEY=AAYS_SAME_PROJECT_NEW_PAGE
PROJECT=AAYS_TerraYield
TASK=AAYS_V28_PARALLEL_SUPERVISION_PLAN
STATUS=v28_parallel_supervision_plan_ready
PROGRESS_PERCENT=91
UPDATED_AT=2026-06-04T12:35:00

PLAN:
- Run only independent read-only or publish-only checks in parallel.
- Keep one main runner and do not overwrite active work.
- Prefer external drive temp storage for large working folders.
- Write every result and every error into docs/chatgpt_status.
- Do not require manual PowerShell output paste.

PARALLEL LANES:
1. Publish/recover local V26 result if it is available.
2. Snapshot runner and queue health.
3. Scan recent result files for errors before starting next work.
4. Check free-space risk and route temp folders to external drive.
5. Run lightweight API health probes only when the API is already running.

NEXT:
- If an existing local importer picks this up, it should create reports.
- If no local importer is active, the next PowerShell request should be one short trigger only.
