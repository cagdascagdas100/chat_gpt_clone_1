task_id=terrayield-050-queue-bridge-self-heal
created_by=chatgpt
priority=critical
mode=queue_bridge_self_heal

OBJECTIVE
Repair the missing handoff between GitHub task files and the existing single local runner. The suspected break is that ChatGPT updates docs/chatgpt_status/current-task.txt in the GitHub repo, while the canonical runner may poll the local bridge queue root C:\AAYS_GITHUB_BRIDGE_CLEAN2\ai-queue instead.

WHY THIS TASK EXISTS
The previous 049 heartbeat task was written to GitHub, but no runner-created heartbeat appeared in docs/chatgpt_status/runner_outputs. That means either the local runner is not running, not pulling GitHub, polling a different local queue path, or not pushing outputs. The current-task explicitly lists queue_root=C:\AAYS_GITHUB_BRIDGE_CLEAN2\ai-queue, so the runner protocol must verify whether queue items must be created under that local path rather than only under docs/chatgpt_status.

STRICT RULES
- Use only the existing single canonical runner.
- Do not start duplicate ad-hoc runners.
- Do not run DB writes, migrations, production deploys, destructive git reset, or fake data generation.
- Do not run heavy TerraYield domain tasks until queue bridge is verified.
- First create GitHub-visible diagnostic outputs.

LOCAL CONSTANTS
repo_root=C:\Users\cagda\Documents\GitHub\AAYS
bridge_root=C:\AAYS_GITHUB_BRIDGE_CLEAN2
canonical_runner=C:\AAYS_GITHUB_BRIDGE_CLEAN2\ai-task-scripts\portable_queue_runner.ps1
queue_root=C:\AAYS_GITHUB_BRIDGE_CLEAN2\ai-queue
local_result_root=C:\AAYS_GITHUB_BRIDGE_CLEAN2\ai-results
github_status_root=docs/chatgpt_status
github_runner_output_root=docs/chatgpt_status/runner_outputs

REQUIRED DIAGNOSTIC OUTPUTS
Write and push:
- docs/chatgpt_status/runner_outputs/terrayield-050-queue-bridge-latest.json
- docs/chatgpt_status/runner_outputs/terrayield-050-queue-bridge-latest.txt
- docs/chatgpt_status/runner_outputs/terrayield-050-queue-bridge-audit.json
- docs/chatgpt_status/runner_outputs/terrayield-050-queue-bridge-audit.txt
- docs/chatgpt_status/runner_outputs/latest_output.json

MINIMUM LATEST JSON
{
  "task_id": "terrayield-050-queue-bridge-self-heal",
  "status": "queue_bridge_checked",
  "overall_progress_percent": 24,
  "phase": "runner_checked_github_to_local_queue_bridge",
  "repo_root_exists": true,
  "bridge_root_exists": true,
  "queue_root_exists": true,
  "canonical_runner_exists": true,
  "current_task_seen_in_repo": true,
  "runner_input_seen_in_repo": true,
  "actual_runner_polling_path": "<path or unknown>",
  "queue_files_seen": ["<names>"],
  "latest_local_result_files": ["<names>"],
  "can_write_github_runner_outputs": true,
  "can_commit_push_outputs": true,
  "next_action": "write_safe_resume_task_to_actual_polling_path"
}

STEP 1 - VERIFY PATHS
1. cd repo_root.
2. git pull --rebase if safe; if not safe, record blocker.
3. Confirm docs/chatgpt_status/current-task.txt contains terrayield-050-queue-bridge-self-heal.
4. Confirm this runner input file exists.
5. Confirm bridge_root, canonical_runner, queue_root, and local_result_root exist.
6. List queue_root files sorted by LastWriteTime.
7. List local_result_root files sorted by LastWriteTime.
8. Inspect portable_queue_runner.ps1 enough to identify what path/file pattern it polls.

STEP 2 - OUTPUT FIRST
Before changing any task behavior, write terrayield-050-queue-bridge-latest.json/txt and latest_output.json with at least progress 24, then commit and push.

STEP 3 - QUEUE BRIDGE AUDIT
Write audit files explaining:
- Does the runner read GitHub docs/chatgpt_status/current-task.txt directly?
- Does the runner read C:\AAYS_GITHUB_BRIDGE_CLEAN2\ai-queue?
- If it reads ai-queue, what exact queue file format/name is expected?
- Does any sync process copy GitHub current-task to ai-queue?
- Why did 049 heartbeat not appear?
- What exact file should ChatGPT create/update next for the runner to consume automatically?

PROGRESS CONTRACT
queued_by_chatgpt=18
queue_bridge_checked=24
protocol_audit_written=40
safe_resume_packet_written=60
output_push_verified=80
ready_for_domain_resume=100
blocked_or_failed=percent_at_failure

SUCCESS CRITERIA
- latest_output.json no longer only shows queued_by_chatgpt.
- queue bridge output exists on GitHub.
- audit identifies actual polling path and file format.
- if current-task is the wrong handoff file, the audit states the correct GitHub-editable file or confirms one-time local bridge command is required.

IF BLOCKED
If runner cannot push, leave local files under docs/chatgpt_status/runner_outputs and local_result_root. If runner never sees this task, no GitHub output will appear and ChatGPT will classify the break as local queue bridge not connected to GitHub-visible current-task.
