# aays1 Post-Fix Runner Evidence Gap Report for Codex

Generated by ChatGPT on 2026-07-07 for PAGE_KEY=aays1.

## Verdict

The safe.directory runner code fix exists, but GitHub does not contain fresh post-fix runtime evidence proving the stable runner is healthy.

Real AAYS/TerraYield work must not start from ChatGPT until fresh GitHub evidence is pushed.

## Active context

- Repo root: `C:\AAYS_WT\AAYS_REPAIR_20260706_1738`
- GitHub repo: `cagdascagdas100/chat_gpt_clone_1`
- Branch: `codex/aays-single-runner-v5-20260706`
- PAGE_KEY: `aays1`
- Required runner: `docs/chatgpt_status/_shared/automation/RUN_SINGLE_AAYS_MULTI_PAGE_QUEUE_RUNNER_STABLE_20260707.ps1`
- Do not use F: as canonical.
- Do not start a parallel runner.
- Keep `final_ready=false` unless real GitHub evidence proves completion.

## What is fixed

Commit: `4cdb83f9668fa97ea2d013f18f8a866486e9a915`

Commit message: `Fix stable runner git safe directory handling`

The fix changes `RUN_SINGLE_AAYS_MULTI_PAGE_QUEUE_RUNNER_STABLE_20260707.ps1` so every git invocation injects `-c safe.directory=<path>` for the working directories. This addresses Git dubious ownership / safe.directory failures without relying on global git config.

The committed report `docs/chatgpt_status/_shared/reports/STABLE_RUNNER_SAFE_DIRECTORY_FIX_20260707.md` says:

- PowerShell parser check passed.
- One-shot stable runner scan with `-NoPush -ScanOnly` returned `exit_code=0`.
- Previous `RUNNER_FATAL: STATUS_FAILED` no longer appeared in that local test output.

## Remaining problem

The fix report explicitly says runtime status/log files were intentionally not committed by the fix.

Therefore, GitHub still does not contain fresh runner runtime evidence after the post-fix time window.

Current GitHub runtime files are stale:

### `docs/chatgpt_status/_shared/status/MULTI_PAGE_latest_status.json`

Observed values:

- `run_id=20260707_121551`
- `checked_at=2026-07-07T09:15:51Z`
- `queue_seen=false`
- `queue_started=false`
- `single_runner_lock_acquired=false`
- `runner_output_uploaded=false`
- `CONTINUE_RUNNER_READY=false`
- `final_ready=false`
- blockers:
  - `RUNNER_ALREADY_ACTIVE`
  - `RUNNER_FATAL: RUNNER_ALREADY_ACTIVE`

This is older than the safe.directory fix and still shows the previous stale/blocked runner state.

### `docs/chatgpt_status/_shared/status/stable_runner_daemon_latest.json`

Observed values:

- `checked_at=2026-07-07T00:58:43.9531785Z`
- `status=runner_loop_completed`
- `runner_exit_code=1`
- output tail references old run `20260707_035841`
- old blockers include:
  - `CONTROLLER_DIRTY_SYNC_SKIPPED`
  - `RUNNER_TASK_FAILED`
  - `BLOCKED_WORKTREE_DIRTY`

This is also older than the safe.directory fix and cannot prove current runner health.

## Why ChatGPT cannot finish this directly

ChatGPT can read/write GitHub files, but cannot inspect or control local Windows processes in `C:\AAYS_WT\AAYS_REPAIR_20260706_1738`.

The missing step is local runtime verification and pushing the resulting status/report/heartbeat evidence to GitHub. That requires Codex/local PowerShell access.

## Required Codex actions

1. Work in `C:\AAYS_WT\AAYS_REPAIR_20260706_1738` on branch `codex/aays-single-runner-v5-20260706`.
2. Confirm the repo contains commit `4cdb83f9668fa97ea2d013f18f8a866486e9a915`.
3. Use only the stable runner:
   `docs/chatgpt_status/_shared/automation/RUN_SINGLE_AAYS_MULTI_PAGE_QUEUE_RUNNER_STABLE_20260707.ps1`.
4. Do not start a second or parallel runner.
5. Verify whether any stale runner PID/lock still exists.
6. If a stale lock exists without a live runner process, clear it through the runner contract.
7. If a live runner exists but is hung/idle, stop it cleanly and restart only the approved stable runner.
8. Run a real post-fix stable runner test that pushes evidence to GitHub. `-NoPush -ScanOnly` is not enough for ChatGPT-side validation.
9. Push fresh files showing the post-fix run, including at minimum:
   - `docs/chatgpt_status/_shared/status/MULTI_PAGE_latest_status.json`
   - `docs/chatgpt_status/_shared/status/stable_runner_daemon_latest.json`
   - a fresh `docs/chatgpt_status/_shared/reports/MULTI_PAGE_runner_output_*.json`
   - relevant `docs/chatgpt_status/aays1/status/`, `reports/`, `heartbeat/`, or `runner_outputs/` evidence
10. Keep safety flags false:
    - `final_ready=false`
    - `fake_data=false`
    - `db_write=false`
    - `migration=false`
    - `production_deploy=false`

## Healthy evidence expected after Codex fix

GitHub should show a post-fix timestamp later than 2026-07-07T15:33:19Z and at least one of these valid states:

### Healthy idle state

- no queued tasks for the relevant page, and
- daemon latest timestamp is fresh, and
- runner exits cleanly with `runner_exit_code=0`, and
- no `RUNNER_ALREADY_ACTIVE` fatal blocker.

### Healthy pickup state

- `queue_seen=true`
- `queue_started=true`
- `single_runner_lock_acquired=true`
- current report file exists in `_shared/reports/`
- daemon latest timestamp is fresh
- no `RUNNER_ALREADY_ACTIVE` fatal blocker
- no fake data / DB write / migration / production deploy

## Current action for ChatGPT

No real page work is started.
No completed marker is written.
No progress metric is increased.
`final_ready=false` remains required.
