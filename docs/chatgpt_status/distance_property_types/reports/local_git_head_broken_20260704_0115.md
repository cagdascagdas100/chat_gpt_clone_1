# Local Git HEAD Broken

page_key=distance_property_types
status=LOCAL_GIT_HEAD_BROKEN_PENDING_TASK_QUEUED
final_ready=false

Local runner input gate was queued to bridge pending. Local Git commit failed with: fatal could not parse HEAD.

Local pull is blocked by staged and unstaged changes. Local push is rejected because remote has new work.

Runner chain remains working. Local Git metadata must be repaired before local files can be pushed.

No fake data. No database write. No schema change. No migration. No production deploy.
