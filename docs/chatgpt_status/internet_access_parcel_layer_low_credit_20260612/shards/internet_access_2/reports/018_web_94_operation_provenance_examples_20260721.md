# Website provenance visibility

The slot website now de-duplicates operation IDs across the core, scope and provenance operation streams and renders operations 1-94 line by line. It also renders seven non-parcel provenance examples, the 20/20 provenance verifier contract, the 21/21 wrapper contract and combined 178/178 checks. The last operation remains blocked pending the existing shared runner. Real parcel examples and business rows remain zero; final_ready remains false.
