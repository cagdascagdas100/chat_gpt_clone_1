AAYS TerraYield ML 150k V11 result

PageKey: AAYS_SAME_PROJECT_NEW_PAGE
Project: AAYS_TerraYield

Checked:
- V10 result TXT read from GitHub
- V10 shows micro batch watchdog was prepared, but code was only in ZIP
- Local bridge cannot run ZIP content until files are copied into the bridge folder

Problem:
- Progress can stall if required runner files are not present inside the active bridge folder
- A watchdog package alone is not enough
- The active runner must first pass a file preflight check

Correction in V11:
- Preflight package created
- It checks whether the 150k CSV, ML runner, queue folder and output folder exist
- It writes v11_preflight_result.txt and v11_preflight_result.json
- It keeps all unsafe actions closed

Safety:
db_write=false
production_deploy=false
migration_ddl=false
fake_data=false
destructive_git=false

Progress: 86
