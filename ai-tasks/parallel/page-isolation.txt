Parallel page isolation rule

One page follows one job.
Each job has its own id, state, and result file.
A delay in one job must not stop another job.
The shared pickup task is only for coordination.
Do not overwrite a running task before reading its result.
Max active jobs: 5.

TerraYield group jobs:
- backend
- frontend
- ops
