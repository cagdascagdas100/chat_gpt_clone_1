name: height_difference_2 datum adjustment probe 038
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived_probe:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Probe completed and archived; runtime receipt already exists.'
