name: AAYS BT5 strict 176-187 visible one-shot
on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/aays-bt5-strict-176-187-main-visible.yml'
permissions:
  contents: write
concurrency:
  group: aays-building-type-5-strict-main-once-176-187
  cancel-in-progress: false
jobs:
  strict-spatial:
    runs-on: ubuntu-24.04
    timeout-minutes: 110
    steps:
      - name: Mark BT5 run started
        uses: actions/github-script@v7
        with:
          script: |
            const owner=context.repo.owner, repo=context.repo.repo;
            const path='docs/chatgpt_status/_shared/slots_21/building_type_5/actions_main_run_176_187_status.json';
            let sha;
            try { const r=await github.rest.repos.getContent({owner,repo,path,ref:'main'}); sha=r.data.sha; } catch(e) { if(e.status!==404) throw e; }
            const body={slot_id:'building_type_5',source_windows:'176-187',state:'RUNNING',run_id:String(context.runId),run_attempt:String(process.env.GITHUB_RUN_ATTEMPT||''),head_sha:context.sha,started_at:new Date().toISOString()};
            await github.rest.repos.createOrUpdateFileContents({owner,repo,path,branch:'main',message:'aays: mark BT5 176-187 run started',content:Buffer.from(JSON.stringify(body)).toString('base64'),...(sha?{sha}:{})});
      - name: Checkout trigger payload
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 2
          path: trigger
      - name: Checkout canonical BT5 branch
        uses: actions/checkout@v4
        with:
          ref: codex/aays-single-runner-v5-20260706
          fetch-depth: 0
          path: canonical
      - name: Install strict spatial runtime
        run: python -m pip install --quiet requests shapely
      - name: Execute registered BT5 payload once
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import base64,re,zlib
          from pathlib import Path
          p=Path('trigger/.github/workflows/aays-bt5-strict-176-187-main-once.yml')
          text=p.read_text(encoding='utf-8')
          m=re.search(r"payload='([^']+)'",text)
          if not m: raise SystemExit('BT5_REGISTERED_PAYLOAD_NOT_FOUND')
          Path('/tmp/run_bt5_176_187.py').write_bytes(zlib.decompress(base64.b64decode(m.group(1))))
          PY
          python /tmp/run_bt5_176_187.py
      - name: Mark BT5 run finished
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const owner=context.repo.owner, repo=context.repo.repo;
            const path='docs/chatgpt_status/_shared/slots_21/building_type_5/actions_main_run_176_187_status.json';
            let sha;
            try { const r=await github.rest.repos.getContent({owner,repo,path,ref:'main'}); sha=r.data.sha; } catch(e) { if(e.status!==404) throw e; }
            const body={slot_id:'building_type_5',source_windows:'176-187',state:'${{ job.status }}'.toUpperCase(),run_id:String(context.runId),run_attempt:String(process.env.GITHUB_RUN_ATTEMPT||''),head_sha:context.sha,finished_at:new Date().toISOString()};
            await github.rest.repos.createOrUpdateFileContents({owner,repo,path,branch:'main',message:'aays: mark BT5 176-187 run finished',content:Buffer.from(JSON.stringify(body)).toString('base64'),...(sha?{sha}:{})});
